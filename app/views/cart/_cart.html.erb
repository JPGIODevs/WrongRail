<%= turbo_frame_tag "cart" do %>
  <strong class="text-white link nav-text">Cart</strong>
  <% if @cart.orderables.count > 0 %>
    <%= render "cart/orderables" %>
    <p><%= @cart.orderables.count %> item(s)</p>
  <% end %> 
<% end %>
